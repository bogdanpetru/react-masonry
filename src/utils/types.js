// @flow

export type stone = {
  width: number,
  height: number,
};

export type position = {
  top: number,
  left: number,
  right: number,
  bottom: number,
};

export type spot = {
  top: number,
  left: number,
  right: number,
  bottom: number,
};
