import getAvailablePositions from '../getAvailablePositions';

describe('getAvailablePositions', () => {
  it('should return positions in correct order', () => {
    const previousRows = [
      { top: 0, left: 0 },
      { top: 0, left: 30 },
      { top: 0, left: 55 },
      { top: 0, left: 80 },
    ];
    const currentRow = [];
    const expected = [{}];
  });
});
